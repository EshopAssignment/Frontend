type ThemeId = "light" | "dark" | "snow" | "idiot";
export type {ThemeId}

export type CookiePrefs = {
    necessary: true;
    analytics: boolean;
    marketing: boolean;
    preferences: boolean;
}
